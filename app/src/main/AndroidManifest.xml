<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.paulaumann.nutrients">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".plan.MainPlanActivity" />
        <activity android:name=".plan.NewEntryActivity" />
        <activity android:name=".history.HistoryActivity" />
        <activity android:name=".dbviewer.DBViewerActivity" />
        <activity android:name=".dbviewer.DBAddEntryActivity" />
        <activity android:name=".nutrients.NutrientsActivity" />
        <activity android:name=".util.FoodActivity" />
        <activity android:name=".util.TodoActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>